name: Preview Environment

# Dispatched from twentyhq/ci-privileged's `preview-env-prepare.yaml`.
#
# Runs the attacker-controlled `docker-compose.yml` and `Dockerfile` from the PR
# head SHA, so this job must be treated as fully compromised: a malicious PR can
# bind-mount the runner host and read anything in the job, including process
# memory belonging to the Actions runner itself.
#
# It therefore holds no GitHub credential of any kind. The hostname is assigned
# by ci-privileged before this job starts and the PR comment is posted there, so
# nothing has to be reported back. The only secret here is this one PR's tunnel
# credentials, whose entire capability is serving traffic on the hostname that
# is already serving this PR's code.
#
# Base images (postgres, redis, node) are pulled anonymously through a public
# pull-through mirror (mirror.gcr.io) configured below, which avoids Docker
# Hub's anonymous rate limits without any credential.
#
# Lives in the PUBLIC ci-public repo so the long keepalive runs on free
# Actions minutes (private repos bill them).

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number on twentyhq/twenty'
        required: true
        type: string
      pr_head_sha:
        description: 'PR head SHA to check out and build'
        required: true
        type: string
      repo:
        description: 'Source repo (owner/name)'
        required: true
        type: string
      hostname:
        description: 'Stable preview hostname for this PR'
        required: true
        type: string
      generation:
        description: 'Renewal generation; 1 is the original build'
        required: false
        default: '1'
        type: string

run-name: Preview pr-${{ inputs.pr_number }} gen-${{ inputs.generation }}

permissions: {}

jobs:
  preview-environment:
    timeout-minutes: 355
    runs-on: ubuntu-latest
    steps:
      - name: Start tunnel
        env:
          CLOUDFLARED_VERSION: '2026.3.0'
          TUNNEL_TOKEN: ${{ secrets[format('PREVIEW_TUNNEL_{0}', inputs.pr_number)] }}
          PREVIEW_HOST: ${{ inputs.hostname }}
          PR_NUMBER: ${{ inputs.pr_number }}
        run: |
          set -euo pipefail

          if [ -z "$TUNNEL_TOKEN" ]; then
            echo "::error::No PREVIEW_TUNNEL_${PR_NUMBER} secret on this repo."
            echo "::error::ci-privileged sets it before dispatching this workflow."
            exit 1
          fi

          sudo curl -fsSL -o /usr/local/bin/cloudflared \
            "https://github.com/cloudflare/cloudflared/releases/download/${CLOUDFLARED_VERSION}/cloudflared-linux-amd64"
          sudo chmod +x /usr/local/bin/cloudflared
          cloudflared --version

          log_file="$RUNNER_TEMP/cloudflared.log"
          : > "$log_file"
          cloudflared tunnel --no-autoupdate --logfile "$log_file" --loglevel info \
            run --token "$TUNNEL_TOKEN" \
            > "$RUNNER_TEMP/cloudflared.stdout" 2>&1 &

          pid=$!
          echo "$pid" > "$RUNNER_TEMP/cloudflared.pid"

          # Wait for the tunnel to register a connection: until it does, the DNS
          # record resolves but Cloudflare answers 1033.
          for _ in $(seq 1 60); do
            grep -q 'Registered tunnel connection' "$log_file" 2>/dev/null && break
            if ! kill -0 "$pid" 2>/dev/null; then
              echo "::error::cloudflared exited before registering a connection"
              cat "$log_file" || true
              exit 1
            fi
            sleep 2
          done

          grep -q 'Registered tunnel connection' "$log_file" \
            || { echo "::error::timed out waiting for tunnel registration"; cat "$log_file" || true; exit 1; }

          echo "Tunnel up at https://${PREVIEW_HOST}"

      - name: Checkout twentyhq/twenty at PR head
        uses: actions/checkout@v5
        with:
          repository: twentyhq/twenty
          ref: ${{ inputs.pr_head_sha }}
          persist-credentials: false

      - name: Route Docker Hub pulls through a public mirror (no credentials)
        run: |
          set -euo pipefail
          echo '{ "registry-mirrors": ["https://mirror.gcr.io"] }' | sudo tee /etc/docker/daemon.json
          sudo systemctl restart docker
          for _ in $(seq 1 30); do docker info >/dev/null 2>&1 && break; sleep 1; done
          docker info | sed -n '/Registry Mirrors/,/Live Restore/p' || true

      - name: Run compose setup
        run: |
          echo "Patching docker-compose.yml..."
          yq eval 'del(.services.server.image)' -i packages/twenty-docker/docker-compose.yml
          yq eval '.services.server.build.context = "../../"' -i packages/twenty-docker/docker-compose.yml
          yq eval '.services.server.build.dockerfile = "./packages/twenty-docker/twenty/Dockerfile"' -i packages/twenty-docker/docker-compose.yml
          yq eval '.services.server.build.target = "twenty"' -i packages/twenty-docker/docker-compose.yml

          yq eval 'del(.services.worker.image)' -i packages/twenty-docker/docker-compose.yml
          yq eval '.services.worker.build.context = "../../"' -i packages/twenty-docker/docker-compose.yml
          yq eval '.services.worker.build.dockerfile = "./packages/twenty-docker/twenty/Dockerfile"' -i packages/twenty-docker/docker-compose.yml
          yq eval '.services.worker.build.target = "twenty"' -i packages/twenty-docker/docker-compose.yml

          echo "Adding SIGN_IN_PREFILLED environment variable to server service..."
          yq eval '.services.server.environment.SIGN_IN_PREFILLED = "${SIGN_IN_PREFILLED}"' -i packages/twenty-docker/docker-compose.yml

          echo "Adding IS_FEATURE_FLAG_MANAGEMENT_ENABLED environment variable to server service..."
          yq eval '.services.server.environment.IS_FEATURE_FLAG_MANAGEMENT_ENABLED = "${IS_FEATURE_FLAG_MANAGEMENT_ENABLED}"' -i packages/twenty-docker/docker-compose.yml

          echo "Setting up .env file..."
          cp packages/twenty-docker/.env.example packages/twenty-docker/.env

          echo "Generating secrets..."
          echo "" >> packages/twenty-docker/.env
          echo "# === Randomly generated secrets ===" >> packages/twenty-docker/.env
          echo "APP_SECRET=$(openssl rand -base64 32)" >> packages/twenty-docker/.env
          echo "PG_DATABASE_PASSWORD=$(openssl rand -hex 16)" >> packages/twenty-docker/.env
          echo "SIGN_IN_PREFILLED=true" >> packages/twenty-docker/.env
          echo "IS_FEATURE_FLAG_MANAGEMENT_ENABLED=true" >> packages/twenty-docker/.env
          echo "Docker compose build..."
          cd packages/twenty-docker/
          docker compose build
        working-directory: ./

      - name: Start services with correct SERVER_URL
        env:
          PREVIEW_HOST: ${{ inputs.hostname }}
        run: |
          cd packages/twenty-docker/

          echo "Setting SERVER_URL to https://$PREVIEW_HOST"
          sed -i '/SERVER_URL=/d' .env
          echo "" >> .env
          echo "SERVER_URL=https://$PREVIEW_HOST" >> .env

          echo "Docker compose up..."
          docker compose up -d || {
              echo "Docker compose failed to start"
              docker compose logs
              exit 1
          }

          echo "Waiting for services to be ready..."
          count=0
          while [ ! $(docker inspect --format='{{.State.Health.Status}}' twenty-db-1) = "healthy" ] || [ ! $(docker inspect --format='{{.State.Health.Status}}' twenty-server-1) = "healthy" ]; do
              sleep 5
              count=$((count+1))
              if [ $count -gt 60 ]; then
                  echo "Timeout waiting for services to be ready"
                  docker compose logs
                  exit 1
              fi
              echo "Still waiting for services... ($count/60)"
          done

          echo "All services are up and running!"
        working-directory: ./

      - name: Seed Dev Workspace
        run: |
          cd packages/twenty-docker/
          echo "Seeding light dev workspace (Apple only)..."
          if ! docker compose exec -T server yarn command:prod workspace:seed:dev --light; then
            echo "❌ Seeding light dev workspace failed. Dumping server logs..."
            docker compose logs server
            exit 1
          fi
        working-directory: ./

      - name: Keep tunnel alive for 5 hours
        run: timeout 325m sleep 19500 # Ends before the 355m job timeout so Cleanup runs

      - name: Cleanup
        if: always()
        run: |
          if [ -f "$RUNNER_TEMP/cloudflared.pid" ]; then
            kill "$(cat "$RUNNER_TEMP/cloudflared.pid")" 2>/dev/null || true
          fi
          if [ -d packages/twenty-docker ]; then
            cd packages/twenty-docker/
            docker compose down -v || true
          fi
        working-directory: ./
